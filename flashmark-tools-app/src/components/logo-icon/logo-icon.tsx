import { component$ } from '@builder.io/qwik';

export interface LogoIconProps {

}

export const LogoIcon = component$<LogoIconProps>((props) => {
  return (
    <div>
      LogoIcon component works!
    </div>
  );
});
